Function: fmaxf

Description: ...

Contract:
Precondition:
	{
	      x == -0 && y == 0 ==> result == 0
          AND x == 0 && y == -0 ==> result == 0
	  AND x == NaN && y != NaN ==> result == y
	  AND x != NaN && y == NaN ==> result == x
	}
	OR
	{
	      x > y ==> result == x
	  AND x <= y ==> result == y
	}

LaTeX:
%\newcommand{\result}{\texttt{result}}
(x = -0 \wedge y = 0 \implies \result = 0) ...

Questions:
What if both are NaN? That's not specified?
